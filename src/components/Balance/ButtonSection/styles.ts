import { RFValue } from "react-native-responsive-fontsize";
import styled from "styled-components/native";

export const ButtonSectionBackground = styled.View`
  width: ${RFValue(100)}%;
  align-items: center;
  flex: 1;
  margin-right: ${RFValue(40)}px;
`;