import { RFValue } from "react-native-responsive-fontsize";
import styled from "styled-components/native";

export const BalancedCardSectionBackground = styled.View`
  width: ${RFValue(100)}%;
  flex: 2;
  align-items: center;
`;