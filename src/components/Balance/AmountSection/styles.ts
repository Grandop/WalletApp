import { RFValue } from "react-native-responsive-fontsize";
import styled from "styled-components/native";

export const AmountSectionBackground = styled.View`
  width: ${RFValue(100)}%;
  padding-top: ${RFValue(5)}px;
  align-items: center;
  flex: 1;
  margin-right: ${RFValue(40)}px;
`;